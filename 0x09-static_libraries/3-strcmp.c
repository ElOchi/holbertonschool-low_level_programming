#include "holberton.h"
/**
 * _strcmp - Sets some elments in memory
 * @s1: memory area to set, the Array
 * @s2: the value we set to the memory area
 *
 * Return: Array with the modified elements
 */
int _strcmp(char *s1, char *s2)
{
return (1);
}
