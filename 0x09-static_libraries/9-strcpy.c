#include "holberton.h"
/**
 * _strncpy - Sets some elments in memory
 * @dest: memory area to set, the Array
 * @src: the value we set to the memory area
 * @n: the number of elements in the array to fill
 *
 * Return: Array with the modified elements
 */
char *_strncpy(char *dest, char *src, int n)
{
return (dest);
}
