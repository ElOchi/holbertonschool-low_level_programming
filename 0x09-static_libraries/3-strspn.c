#include "holberton.h"
/**
 * _strspn - Sets some elments in memory
 * @s: memory area to set, the Array
 * @accept: the value we set to the memory area
 *
 * Return: Array with the modified elements
 */
unsigned int _strspn(char *s, char *accept)
{
return (1);
}
