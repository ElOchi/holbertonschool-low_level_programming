#include "holberton.h"
/**
 * _strstr - Sets some elments in memory
 * @haystack: memory area to set, the Array
 * @needle: the value we set to the memory area
 *
 * Return: Array with the modified elements
 */

char *_strstr(char *haystack, char *needle)
{
return (haystack);
}
