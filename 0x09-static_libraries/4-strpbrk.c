#include "holberton.h"
/**
 * _strpbrk - Sets some elments in memory
 * @s: memory area to set, the Array
 * @accept: the number of elements in the array to fill
 *
 * Return: Array with the modified elements
 */
char *_strpbrk(char *s, char *accept)
{
return (s);
}
